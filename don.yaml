server:
  applicationConnectors:
    - type: http
      port: 8080

  requestLog:
    appenders: []

auth: "your_token" # CHANGE THIS! Put your Service auth token here
cryptoDir: ./data  # path to dir where to store crypto box files
