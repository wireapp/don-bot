server:
  applicationConnectors:
    - type: http
      port: 8080

  requestLog:
    appenders: []

auth: "your_token" # CHANGE THIS! Put your Service auth token here
cryptoDir: /var/lib/don/crypto  # path to dir where to store crypto box files
pathPubKey: '/etc/don/pubkey.pem' # for the channels - not in use right now
botName: Don